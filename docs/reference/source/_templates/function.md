# nx::namespace::Class::function
<!-- @category: function -->

## シグネチャ
```cpp
戻り値 function(引数);
```

## 概要
この関数が何を行うかを簡潔に記述する。

## 戻り値
戻り値の意味を記述する。

## 計算量
必要な場合のみ記述する。（例: O(1), O(n)）

## 例外
例外仕様やエラー条件を記述する。なければ省略可。

## 備考
前提条件・副作用・呼び出し順序の注意点を記述する。不要なら省略可。

## サンプルコード
```cpp
// 標準ライブラリ以外の namespace は using で省略
```
